change all interfaces to private

make vue's url property ''

IMPORTANT CONFIGS:
    -DATASOURCE
    -CORS
    -SYSTEM PROPERTY (IN STANDALONE.XML): sip.uploadDir (set path to correct dir)
    -IMAGE HANDLER (undertow) (IN STANDALONE.XML):  path MUST MATCH ABOVE PATH